import { Layers } from "lucide-react";
import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function DatasourcesPage() {
  return <PlaceholderPage title="Datasources" icon={Layers} />;
}
