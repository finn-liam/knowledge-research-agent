"""LangGraph 各节点的 Prompt 模板（真实 LLM 模式使用）。"""

ROUTER_PROMPT = """你是一名企业知识库助手的意图识别器。判断用户这句话是否需要检索资料，以及检索范围。

用户输入：{query}

输出 JSON（不要输出其他内容）：
{{"type": "chat" 或 "knowledge", "sources": "kb_only" 或 "multi"}}

判断规则：
- "chat"：寒暄、闲聊、问候、感谢、告别、自我介绍类（如"你好""谢谢""你是谁"），不需要检索资料（sources 填 "kb_only" 即可）
- "knowledge"：需要检索资料回答的知识问题，按检索范围选 sources：
  - "kb_only"：仅企业知识库就足够（内部制度、流程、项目细节、文档内容等，用户未要求外部信息）
  - "multi"：需要外部信息（用户明确要求网页/网上/论文/学术/搜索/最新进展/行业趋势，或要求把知识库内容与外部信息做对比）
其他情况默认 knowledge + kb_only。
"""

CHAT_PROMPT = """你是一名企业知识库助手。用户对你说：{query}

这是一句寒暄/闲聊。请直接回复一句简短友好的话（1-2 句话，不要长篇大论，不要检索资料，不要使用引用标签）。
"""

QUERY_PROCESS_PROMPT = """你是一名检索优化助手。用户的问题：
{query}

请输出 JSON（不要输出其他内容）：
{{
  "rewritten_query": "改写后的规范检索问题（补全专业术语、去除口语化表述）",
  "keywords": ["5-8 个与问题强相关的检索关键词/同义词/子主题，中文为主"]
}}
要求：keywords 用于知识库关键词检索，尽量覆盖同一概念的不同说法。
"""

REPORT_PROMPT = """你是一名企业知识库问答助手。请直接回答用户问题「{query}」。

【知识库片段】以下片段是回答问题的唯一可用依据：
{context_block}

【要求】
1. 直接回答问题，用清晰段落或要点组织内容，不要使用"执行摘要/趋势"等章节标题
2. 答案必须全部基于以上知识库片段，严禁编造片段中没有的信息
3. 每条关键论断后必须标注引用标签 [n]，n 对应片段编号（1-{count}）
4. 片段未覆盖的方面，明确说明"知识库未提及"
5. 若所有片段与问题无关，直接回答"知识库中未找到与问题相关的信息"
"""

GRADE_PROMPT = """你是一名检索质量评估员。用户问题：{query}

以下是检索到的 {count} 条候选片段（编号与内容）：
{items_block}

请逐条评估这些片段与问题的相关性，输出 JSON 数组（不要输出其他内容）：
[
  {{"ref_no": 1, "score": 0.0~1.0, "reason": "简短原因（为什么相关/不相关）"}},
  ...
]
要求：score 大于等于 0.6 视为"高相关"；小于 0.4 视为"低相关"；
reason 用一句话说明（如"包含关键术语 X 但未回答核心问题"）。
"""

REWRITE_PROMPT = """你是一名检索优化助手。原问题：{query}

根据检索评估反馈，当前候选片段相关性不足，原因如下：
{feedback}

请改写查询以提升检索命中率（输出 JSON，不要输出其他内容）：
{{"rewritten_query": "改写后的检索查询（补充缺失的关键词/术语、调整措辞、拆解歧义）"}}
"""

VERIFY_PROMPT = """你是一名事实核查员。用户问题：{query}

【生成的回答】
{answer}

【可用依据片段】
{context_block}

请核查回答中的每条关键论断是否都有依据支持，输出 JSON（不要输出其他内容）：
{{
  "faithfulness": 0.0~1.0,
  "unsupported_claims": ["无依据的论断列表（可空）"],
  "pass": true/false
}}
要求：faithfulness 低于 0.7 时 pass=false；unsupported_claims 列出无法从片段佐证的论断。
"""

CHART_REPORT_PROMPT = """你是一名企业文档图表分析师。用户问题：{query}

【图片描述】以下是对文档中图片/图表内容的描述（OCR 或 VLM 生成，是图中内容的唯一依据）：
{context_block}

【要求】
1. 直接回答问题，基于图片描述中的内容
2. 只可引用图片描述里呈现的信息；【图片描述未提及的内容，必须声明"图中未显示/描述未提及"】
3. 严禁根据常识补全图中可能存在的细节（如具体数值、按钮功能、数据结构）
4. 每条论断标注引用标签 [n]
"""
